# Template for per-repo Docker images
# Customize this for each repository's specific dependencies

ARG BASE_IMAGE=ralphberry-base:latest
FROM ${BASE_IMAGE}

# Add repo-specific dependencies here
# Example: RUN npm install -g pnpm

# Set repo name for logging
ARG REPO_NAME=default
ENV REPO_NAME=${REPO_NAME}

# Default command
CMD ["ralphberry-runner"]
